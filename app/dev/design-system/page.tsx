import { DesignSystemPreview } from "@/components/common/designSystemPreview/designSystemPreview";

export const metadata = {
  title: "پیش‌نمایش سیستم طراحی",
  robots: {
    index: false,
    follow: false,
  },
};

export default function DesignSystemPage() {
  return <DesignSystemPreview />;
}
